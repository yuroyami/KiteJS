POM_NAME=KiteJS
POM_DESCRIPTION=JavaScript engine for Kotlin Multiplatform: an ECMAScript interpreter ported from Mozilla Rhino, running in pure common Kotlin on Android, iOS, JVM and JS. Pre-alpha; rewrite this description before the first publish so it only claims what the code does.
